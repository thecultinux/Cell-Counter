#ifndef MORPHOLOGY_H
#define MORPHOLOGY_H

image erode(image input, image output);
image expanse(image input, image output);
image reconstruct(image fichier, image seed, image output);
image ultimate_erode(image input, image output);
#endif
